# Phase 1: data generation + validation
numpy>=1.24
scipy>=1.10
numba>=0.58
h5py>=3.9
matplotlib>=3.7
tqdm>=4.66

# Phase 3: validation notebook -- install when needed
#   pip install notebook ipykernel
# Phase 4+: CNN training. On Jetson Nano use the NVIDIA-prebuilt wheel.
#   pip install torch
